/**
 * Scheduling links used across the host flow.
 *
 * CALENDLY_URL powers both the "Learn more → Schedule a call" flow and the
 * required prize-review call shown after submitting a competition whose cash
 * prize is over the review threshold.
 */
export const CALENDLY_URL = 'https://calendly.com/crystal-mosteligibleusa/eliterank-info-session';
